$ git --version
$ mkdir test1
$ cd test1
$ git init
$ touch demo.txt
$ nano demo.txt
$ cat demo.txt
$ git status
$ git add demo.txt
$ git commit -m "commiting a text file"
$ git config --global user.username "beingshrikant17"
$ git remote add origin https://github.com/beingshrikant17/wad_assignment.git
$ git push origin master
